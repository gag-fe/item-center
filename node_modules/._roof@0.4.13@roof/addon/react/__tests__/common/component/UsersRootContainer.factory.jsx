var React = require('react')
var Roof = require('roof')

var Users = require('./UsersContainer.jsx')
var RootContainerFactory = require('./RootContainer.factory.jsx')


module.exports = function( data, events  ){
  return RootContainerFactory( data, events, Users)
}