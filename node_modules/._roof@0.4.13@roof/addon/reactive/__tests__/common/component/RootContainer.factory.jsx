var React = require('react')
var Roof = require('roof')

module.exports = function( data, events , Component ){
  return Roof.createRootContainer({
    events : events || {},
    data : data || data,
    render(){
      return <Component />
    }
  })
}