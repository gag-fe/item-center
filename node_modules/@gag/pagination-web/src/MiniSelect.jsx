import React from 'react';
import Select, { OptionProps } from '@gag/select-web';

export default class MiniSelect extends React.Component {
  static Option = Select.Option;

  render() {
    return <Select size="small" {...this.props} />;
  }
}
